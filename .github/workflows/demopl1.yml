name: my first workflow
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
        - name: Welcome msg
          run: echo "My first github action job"
        - name: list files
          run: ls